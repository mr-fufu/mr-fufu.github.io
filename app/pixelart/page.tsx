import { pixelartImages } from "../images";
import { GalleryPage } from "../galleryPage";

export default function PixelArt() {
  return (
    <GalleryPage pageTitle="PIXEL ART" images={pixelartImages} />
  )
}